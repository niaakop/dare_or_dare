<h1>Edit Dare</h1>

<%= form_with(model: @dare, local: true) do |form| %>
  <% if @dare.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dare.errors.count, "error") %> prohibited this dare from being saved:</h2>
      <ul>
        <% @dare.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :template, "Dare Template" %>
    <%= form.text_area :template, rows: 5 %>
  </div>

  <div class="actions">
    <%= form.submit "Update Dare", class: 'btn btn-primary' %>
  </div>
<% end %>

<%= link_to 'Back to Dares', dares_path, class: 'btn btn-secondary' %>
